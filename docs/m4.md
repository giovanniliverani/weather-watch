# M4: summaries and numbers, under a hard budget

Generated 2026-09-25T20:24:58Z by `eww eval extraction --backend local` against `/Users/giovanniliverani/Documents/Repositories/weather-watch/data/eww.sqlite`.

## Golden set

30 documents (`tests/golden/documents.jsonl`) and 10 events (`tests/golden/events.jsonl`). A figure is kept only when its evidence span is a verbatim substring of the title, the excerpt or the authority text; a place is kept only when it geocodes within twice the candidate event's blocking radius. Where the lexicon already named a hazard, that type is the one scored.

| Measure | Value |
|---|---:|
| Hazard accuracy | 30 / 30 (100.0%) |
| Place resolution | 30 / 30 (100.0%) |
| Figures exact-match | 40 / 40 (100.0%) |
| Evidence-span violations in the kept output | 0 |
| Cost of the run | $0.0000 |
| Model calls | 40 |

## Exit criteria

| Criterion | Result |
|---|---|
| hazard accuracy >= 90% | PASS (100.0%) |
| place resolution >= 80% | PASS (100.0%) |
| figures exact-match >= 80% | PASS (100.0%) |
| every kept figure span is verbatim | PASS (0) |

## This database

| Measure | Value |
|---|---|
| Month-to-date `llm_call.cost_usd` | $0.0000 (cap $10) |
| Rows with `backend = 'anthropic'` this month | 0 |
| Documents awaiting extraction older than 24 h | 0 |
| Evidence-span violations | 0 |

```
month spend $0.0000 <= 10: PASS
evidence-span violations 0: PASS
documents awaiting extraction > 24 h: 0
```

## How to re-measure

1. Edit the labels in `tests/golden/documents.jsonl` and `tests/golden/events.jsonl`.
2. Re-run `uv run eww eval extraction` (the default backend is `local`; `ollama` and `anthropic` are optional, and Anthropic still stops at the cap).
